# Code Review State: global / m1-task6-orphan-watchdog

Last reviewed: 2026-09-02
Rounds completed: 1

## Resolved (fixed in code; do not re-raise)
- [cr-C3] **The rule the class claims to keep was inoperative in production, and the test covering it exercised a path the shipped runtime does not take.** `Finalization` has THREE outcomes; the watchdog treated only a THROW as a failed salvage. The Docker arm — the only shipped one — reports "could not observe the agent" as DATA (`faulted`, `overran`) in four places and raises only on the genuinely unexpected. So a unit whose agent nobody could read was destroyed, removing the evidence salvage exists to keep. The decision is now made on the salvage's VALUE, and a kept unit is also STOPPED, because "an unobservable agent has already exited" is one arm's private promise — round 1
- [cr-C1 / qa-MED-1] `destroy` swallowed its failure and `leases.release` ran on the next line anyway, so a container that survived teardown lost the only row naming its run. **Three comments described the recovery the code removed.** QA measured it: `released=[…] leaseStillPresent=false`. `destroy` reports a boolean and the release is gated on it — round 1
- [cr-C2 / sec-H1 / qa-MED-1] The `REAP_SLOT` claim gated the whole reap rather than the report, so one transient daemon blip permanently disqualified a credential-bearing sandbox from ever being reclaimed — with the mechanism built to find it logging "already reported" at DEBUG. The claim now sits inside `report`; the teardown is retried while the unit is there — round 1
- [sec-M3] **Every preserved unit got a second, contradictory terminal result.** Its run is over and the dispatcher already published the outcome; the watchdog then reported a retryable `SANDBOX_LOST` the launcher's own rules forbid, and landed an UNKNOWN-priced charge line on a run whose spend was already recorded — an UNKNOWN TOTAL line does not collide with the real INPUT/OUTPUT rows under the ledger's unique key. A preserved unit is now reclaimed without a second report — round 1
- [cr-I5] **The two "never reaped" tests were the same test.** Both asserted "a fresh heartbeat protects a run"; the watchdog never read `ownerId` at all, so the pair gave false confidence about in-process safety specifically. Replaced with the guard that cannot be wrong: a run this process is executing is never an orphan, taken from the in-memory registry. The gap was reachable — a database outage longer than the staleness window ages every lease this replica holds while its runs work perfectly well — round 1
- [cr-I2 / sec-M1] Staleness was judged against the JVM clock while `heartbeat_at` is written with the database's, contradicting the invariant `WorkspaceLeases` states in its own javadoc. A worker running ahead of the database reads every live lease as abandoned — silently, in the destroying direction. The horizon is read from the database once per tick, which also gives the whole sweep one consistent instant — round 1
- [qa-HIGH-1] **Deleting `@Scheduled` from the sweep left all 17 suites of the module green** while the watchdog never ran and the leak it exists to close came back. No boot test can catch it, because the test profile disables the scheduler. `ScheduledWorkIsDeclaredTest` now covers both timers — the module had seven scheduled methods and one guard — round 1
- [qa-HIGH-2] The startup refusal was protected against being mis-wired (any boot test catches that for free) and not against ceasing to exist: emptying the observer's body left the suite green. Driven through `check(null)` on both sides now — round 1
- [qa-MED-2 / cr-I7 / rules-2] `RunResultReporter` had zero coverage, because a test-only `Consumer` field in production code short-circuited it in every case. The field is deleted, the fake subclasses the bean like the other two, and the reporter has its own file: publish, refusal, bounded wait, interrupt, and both halves of the new budget refusal. The fake needs `@Vetoed` — the startup observer makes its declaring class a bean AND is inherited, so without it the container finds two reporters and every injection is ambiguous — round 1
- [cr-I3 / qa-LOW-2 / rules-10] `spire.run.lease-heartbeat` had two independent inline defaults, so the startup guard could validate a number the scheduler did not use — the exact fault the guard exists to catch, reproduced inside it. Every run property is declared once in `application.yml`; `result-ack-seconds` is one number both publish paths read, replacing a constant in one and a property in the other — round 1
- [cr-I4] Nothing could turn the watchdog off, and it destroys containers on a timer. `SPIRE_RUN_ORPHAN_WATCHDOG_ENABLED`, logged at startup when off so it cannot be disabled and forgotten — the precedent the symbol index set — round 1
- [sec-M5] The reclaimed failure bypassed `RunFailures`, the class documented as the one place a failure becomes a `RunFailed`. The clip is shared with a javadoc stating why the credential scrub cannot apply — the watchdog holds no command, so it has no credential refs to decrypt, and its details are runtime constants plus an exception class name — round 1
- [sec-L1 / S4] `result-ack-seconds` was unvalidated: a zero times out instantly, losing every report while the configuration looks set. Refused at startup — round 1
- [rules-1] `InterruptedException` logged without the exception — round 1
- [rules-3] Eight new log lines carried the run id in the message with no MDC, in the only run-scoped lines in the module that could not be queried by `runId`. The sweep wraps each unit — round 1
- [rules-5] `docs/factory/ARCHITECTURE.md`'s normative orphan definition lacked the `preserved` arm that this task checks FIRST, and that definition is what a future reader builds from — round 1
- [rules-6] `WorkspaceLeasesIT`'s javadoc said `discoverUnits` "is called from no production code", falsified by this very commit — round 1
- [qa-LOW-1] Nothing asserted the reclaimed run's usage stays unknown rather than zero, or that it is retryable — round 1
- [qa-LOW-3 / cr-S5] Two duplicate tests and two dead lines — round 1
- [qa-LOW-4] A stale `discoverOrphans` in an open debt entry Task 7 is being implemented from — round 1
- [qa-LOW-5] The staleness boundary was asserted at ±1 minute rather than at the threshold — round 1
- [cr-S2 / S3] `units()` returns `List`; `MISSED_HEARTBEATS_TOLERATED` moved to the constants and its javadoc narrowed to the case a multiplier can actually cover — round 1

## Dismissed (acknowledged, will not fix; agents may escalate with explicit justification)
- [cr-S1] Extract the publish primitive so the dispatcher and the reporter share one catch ladder. The ack budget is now shared, which was the drift that mattered; merging the ladders is a refactor of a path the dispatcher's compaction depends on, and belongs on its own (round 1)
- [rules-13] No Conventional-Commits prefix, body wrapping past 72. House style across 100+ commits; the user's own instructions constrain the subject line only (round 1)

## Open (tracked; not fixed in this round)
- [sec-M4 / rules-4] **`2-4` was deleted with one arm uncovered.** The sweep is driven by the unit listing, so the reverse direction — a lease with no unit, the window the take-before-create ordering opens on purpose — is reclaimed by nothing, and `WorkspaceLeases.staleLeases` (written for exactly this, with a javadoc naming the watchdog) has no production caller. The run's row stays `queued` forever. The credential half IS closed, because a leaked container always carries the label. Restored as a scoped entry
- [cr-I1] The sweep has no time budget: `salvage` on a lease-less unit whose agent is still running blocks for that unit's wall clock, and under `SKIP` every other orphan waits behind it
- [sec-L3] Replicas sharing one daemon must share one `runworker` schema, or each sees the other's units as lease-less and destroys them. Undocumented
- [sec-L1] `verify` has no absolute floor: a one-second heartbeat with a four-second threshold passes the ratio
- `docs/SMOKE-TEST.md` Mode Q presents the container label as the operator's recourse for a preserved unit, whose inspection window is now bounded by one sweep. Due with the M1 documentation pass

## Notes
- **Three lenses reached the same two defects, and QA proved both by probe rather than by reading.** It also corrected a premise in my own review brief — I told it the lease was NOT released on a failed destroy, and it measured that it was.
- **The sharpest finding was on nobody's list of questions.** `Finalization`'s three outcomes collapsed to two meant the class's headline rule was enforced only on a path the shipped runtime avoids. That is the same seam shape this milestone has now hit four times: a fake exercising a door production does not use.
- QA reported the build lane being used underneath it for the third consecutive round, and this time it was unambiguously me — I edited the worktree mid-build. Its numbers survived only because it moved to a pristine `git archive` copy and checked timestamps. It also caught that a plain `testFast` returned BUILD SUCCESSFUL in two seconds with zero tests executed, which any of us could have read as green.
- Eleven mutations on the original, and the fix batch is covered by tests that fail against the code they replace.
